$kind: collection
description: API collection for the Ecommerce Spring Boot application. This
  collection includes endpoints for authentication, users, roles, shops, shop
  locations, categories, products, variants, and variant values.
variables:
  baseUrl: http://localhost:8080
